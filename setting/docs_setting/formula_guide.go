package docs_setting

const formulaGuideMarkdown = `# 计费公式编写说明

这份文档说明本站「按公式」售价和成本公式怎么写。可以直接复制整页给 AI，让它按这些规则出公式。

## 三层计费（不要写错层）

1. **底价**：你写的公式，输出美元金额（必须用 ` + "`v2:`" + ` 前缀）。
2. **售价额度**：底价 × 分组倍率 × 用户模型倍率。未配置的倍率当 1，用户倍率允许为 0。
3. **成本额度**：成本公式单独跑。永不乘分组倍率，永不乘用户模型倍率。成本算失败只记 unknown，不改客户扣费。

公式字符串里禁止写分组倍率或用户倍率。系统会在公式外面乘。

失败退款和成功后的多退少补，退的都是第 2 层售价额度，不是底价。

## 版本

- ` + "`v2:0.08`" + `：0.08 美元，再按站点额度换算。按次、按秒、按张都用 v2。
- 不写 ` + "`v2:`" + ` 时按 v1，表示「每百万 token 的美元价」，给对话模型用。

## 读请求和响应

| 函数 | 作用 |
| --- | --- |
| ` + "`param(\"path\")`" + ` | 读客户请求 JSON，路径用 gjson |
| ` + "`resp(\"path\")`" + ` | 读上游响应或任务轮询 JSON。提交预扣时为空 |
| ` + "`count(\"path\")`" + ` | 列表长度，或数字字段。缺失为 0，上限 256 |
| ` + "`num(a, b, 6)`" + ` | 取第一个有效数字，否则用后面的缺省 |
| ` + "`coalesce(a, b)`" + ` | 第一个非空值 |
| ` + "`usage(\"prompt\")`" + ` | 用量别名 |
| ` + "`max` / `min` / `abs`" + ` | 常用数学 |

提交时用 ` + "`param`" + ` 预扣。任务成功后同一条公式用 ` + "`resp`" + ` 重算，多退少补。

## 计费专用字段（不会发给上游）

` + "`input_seconds`" + ` 只给本站公式用，不会出现在发给上游的 JSON 里。

- 请求里没有参考视频（没有 ` + "`video_url`" + ` / ` + "`content[].type=video_url`" + `）：按 0。
- 有参考视频且客户没传：按 15。
- 客户传了：用传入值（不能为负，上限与任务时长上限相同）。

公式里写 ` + "`param(\"input_seconds\")`" + ` 即可。不要让上游文档出现这个字段。

## 可复制示例

按次：

` + "```" + `
v2:0.04
` + "```" + `

按请求秒数：

` + "```" + `
v2:num(param("seconds"), param("duration"), 6) * 0.08
` + "```" + `

列表超过 5 项加价：

` + "```" + `
v2:0.04 + max(count("images") - 5, 0) * 0.01
` + "```" + `

任务：预扣读请求，结算读轮询（路径按你的上游文档改）：

` + "```" + `
v2:num(resp("task.usage.output_seconds"), param("duration"), 6) * 0.08 + num(resp("task.usage.input_seconds"), param("input_seconds"), 0) * 0.08 + max(num(resp("task.usage.input_image_count"), count("content.#(type==\\"image_url\\")#"), 0) - 5, 0) * 0.04
` + "```" + `

把 0.08、0.04 和 JSON 路径换成你自己的单价和上游字段。不要把上游返回的 cost 直接当售价，除非你明确要做「成本转售」。

## 写给 AI 的约束

- 只输出一条 ` + "`v2:`" + ` 公式，不要把分组或用户倍率乘进去。
- 成本公式另写一条，同样不要乘那两项。
- 结果必须恒为非负数。
- 任务模型要同时考虑预扣（resp 为空）和结算（resp 有值），用 ` + "`num(resp(...), param(...), 缺省)`" + `。
- 参考视频秒数用 ` + "`input_seconds`" + `，不要假设可以下载 URL。
`
